<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.preetham.organic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".IntroActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignupActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="Organic"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".VegeActivity"
            android:label="Organic" />
        <activity
            android:name=".ProfileActivity"
            android:label="Organic" />
        <activity android:name=".ProfileUpdateActivity" />
        <activity android:name=".CarrotActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".ConfirmActivity" />
        <activity android:name=".Place_Order_Activity"></activity>
    </application>

</manifest>